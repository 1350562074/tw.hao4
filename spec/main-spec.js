const main = require('../main/main');
describe('main()', () => {
   /* it('should output the right outcome',function()
{
    var inputs=['3','2','1','4'];
    var summary = main(inputs).trim();
    var expected = '2A2B'.trim()
    expect(summary).toEqual(expected);
})*/

});